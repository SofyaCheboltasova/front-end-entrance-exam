import Box from "../../components/box/box";
import Modal from "../../components/modal/modal";
import { tools } from "../../public/boxData";

export default function getToolsBox() {
  const toolsBox = new Box();

  tools.forEach((tool) => {
    const modal = new Modal();
  });
}

